import calculator from "./InitialState"

const name = (state = calculator, {ty}) => {
    switch (action.type) {
        case ACTION_TYPE_1:
            return state
        default:
            return state
    }
}