import React from 'react'
import { connect } from 'react-redux'

export const Butttons = (props) => {
  return (
    <div>Butttons</div>
  )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(Butttons)